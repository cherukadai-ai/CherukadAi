"""Agents module (platform_core boundary).

Public surface: import from `app.modules.agents.api` and `app.modules.agents.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
