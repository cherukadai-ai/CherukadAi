"""Identity module (platform_core boundary).

Public surface: import from `app.modules.identity.api` and `app.modules.identity.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
