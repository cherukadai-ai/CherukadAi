"""Files module (platform_core boundary).

Public surface: import from `app.modules.files.api` and `app.modules.files.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
