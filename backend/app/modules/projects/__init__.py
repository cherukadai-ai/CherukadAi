"""Projects module (platform_core boundary).

Public surface: import from `app.modules.projects.api` and `app.modules.projects.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
