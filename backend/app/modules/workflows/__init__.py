"""Workflows module (platform_core boundary).

Public surface: import from `app.modules.workflows.api` and `app.modules.workflows.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
