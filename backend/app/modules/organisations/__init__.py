"""Organisations module (platform_core boundary).

Public surface: import from `app.modules.organisations.api` and `app.modules.organisations.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
