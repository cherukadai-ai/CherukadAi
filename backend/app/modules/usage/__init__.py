"""Usage module (platform_core boundary).

Public surface: import from `app.modules.usage.api` and `app.modules.usage.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
