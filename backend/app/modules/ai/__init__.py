"""Ai module (platform_core boundary).

Public surface: import from `app.modules.ai.api` and `app.modules.ai.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
