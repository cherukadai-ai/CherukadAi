"""Ai Orchestration module (platform_core boundary).

Public surface: import from `app.modules.ai_orchestration.api` and `app.modules.ai_orchestration.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
