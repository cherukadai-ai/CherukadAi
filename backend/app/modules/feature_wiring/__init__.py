"""Feature Wiring module (platform_core boundary).

Public surface: import from `app.modules.feature_wiring.api` and `app.modules.feature_wiring.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
