"""Permissions module (platform_core boundary).

Public surface: import from `app.modules.permissions.api` and `app.modules.permissions.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
