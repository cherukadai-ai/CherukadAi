"""Audit module (platform_core boundary).

Public surface: import from `app.modules.audit.api` and `app.modules.audit.domain`
only. Other modules must not reach into `application`/`infrastructure` directly.
"""
